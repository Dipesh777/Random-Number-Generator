

const intialNumValue = []

const numberReducer = (state = intialNumValue, action) => {
    switch (action.type) {

        default: {
            return [...state]
        }
    }
}

export default numberReducer